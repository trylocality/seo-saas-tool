# Webhook Configuration Example
# Copy these to your .env file and replace with your actual Zapier webhook URLs

# Email Verification Webhook
# Used when users sign up or request verification email resend
EMAIL_VERIFICATION_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/YOUR_ZAPIER_ID/YOUR_WEBHOOK_ID/

# Password Reset Webhook
# Used when users request password reset
PASSWORD_RESET_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/YOUR_ZAPIER_ID/YOUR_WEBHOOK_ID/

# Feedback Submission Webhook
# Used when users submit feedback (already configured)
FEEDBACK_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/23916813/uudm9dj/

# New User Alert Webhook
# Used when new users sign up
NEW_USER_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/YOUR_ZAPIER_ID/YOUR_WEBHOOK_ID/

# Bulk Audit Completion Webhook
# Used when bulk audits are completed to notify users
BULK_AUDIT_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/YOUR_ZAPIER_ID/YOUR_WEBHOOK_ID/

# Generic Email Webhook (optional)
# Used as fallback for email verification and password reset if specific webhooks not set
EMAIL_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/YOUR_ZAPIER_ID/YOUR_WEBHOOK_ID/